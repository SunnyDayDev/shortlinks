## ADDED Requirements

### Requirement: Тихое фоновое открытие
Приложение SHALL быть фоновым агентом (`LSUIElement` / политика активации
`.accessory`) и при открытии `sl://link/<slug>`, не требующем взаимодействия с
пользователем, SHALL выполнять переход **без иконки в Dock и без вывода окна**.
Открытие без взаимодействия — это активная многоразовая ссылка без пароля и при
выключенном режиме подтверждения. Поскольку macOS резолвит кастомную схему через
LaunchServices, запуск процесса-обработчика неустраним; требование ограничивает
видимость этого запуска, а не сам факт.

#### Scenario: Фоновый переход без UI
- **WHEN** открывается `sl://link/<slug>` активной многоразовой ссылки без пароля при выключенном режиме подтверждения
- **THEN** цель открывается, окно не выводится и иконка приложения не появляется в Dock

#### Scenario: Нет постоянной иконки в Dock
- **WHEN** приложение запущено и обрабатывает `sl://` в фоне
- **THEN** оно присутствует только в меню-баре, постоянной иконки в Dock нет

### Requirement: Временное повышение для взаимодействия
Приложение SHALL временно повышать политику активации до `.regular`, показывать окно
оверлея и активироваться, когда обработка `sl://` требует взаимодействия (пароль,
ненайденная или заблокированная ссылка, включённый режим подтверждения), а после
завершения или отмены перехода SHALL возвращаться в `.accessory`.

#### Scenario: Запрос требует UI
- **WHEN** открывается `sl://link/<slug>` защищённой, ненайденной или заблокированной ссылки (либо включён режим подтверждения)
- **THEN** приложение показывает окно оверлея и активируется поверх других окон

#### Scenario: Возврат в фон после перехода
- **WHEN** оверлей перехода закрывается (переход выполнен или отменён)
- **THEN** приложение возвращается в фоновый режим без иконки в Dock
