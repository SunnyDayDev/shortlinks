## 1. Перевод приложения в agent-режим

- [x] 1.1 В `project.yml` выставить `LSUIElement: true` и обновить комментарий
  (агент в меню-баре, динамическое повышение политики при UI).
- [x] 1.2 Прогнать `xcodegen generate`; синхронизировать `Sources/ShortlinksApp/Info.plist`
  (`LSUIElement` → `<true/>`), если он остаётся источником под VCS.

## 2. Динамическая политика активации

- [x] 2.1 В `AppDelegate.application(_:open:)`: при `surfaceUI` —
  `NSApp.setActivationPolicy(.regular)`, активировать и показать окно; иначе фоновый
  переход без вывода окна и без повышения политики.
- [x] 2.2 Реализовать возврат в `.accessory` после закрытия оверлея перехода
  (по обнулению состояния редиректа в `AppModel` или закрытию окна `main`).
- [x] 2.3 Убедиться, что пункты меню-бара и онбординг/настройки, открывающие окно,
  повышают политику до `.regular` перед показом.

## 3. Проверка сценариев

- [x] 3.1 Фоновый переход (активная многоразовая без пароля): цель открывается, окна
  и иконки в Dock нет.
- [x] 3.2 Пароль / ненайденная / заблокированная / режим подтверждения: окно
  показывается поверх и с фокусом ввода, после закрытия — возврат в фон.
- [x] 3.3 Открытие главного окна из меню-бара работает; постоянной иконки в Dock нет.

## 4. Документация

- [x] 4.1 Обновить `CLAUDE.md` (модуль ShortlinksApp + «Ключевые решения»):
  agent-режим (`LSUIElement`/`.accessory` + динамическое повышение) и неустранимая
  причина запуска обработчика схемы (LaunchServices).
- [x] 4.2 `/opsx:sync` — перенести дельту в `openspec/specs/link-redirection`.
