## 1. Вложение CLI в бандл (сборка)

- [ ] 1.1 В `project.yml` добавить app-таргету зависимость на `shortlinks-cli` с copy-фазой в destination `executables` (`Contents/MacOS`) и `codeSign: true`
- [ ] 1.2 `xcodegen generate` и сборка `ShortlinksApp`; убедиться, что в `Shortlinks.app/Contents/MacOS/shortlinks` лежит подписанный исполняемый бинарь
- [ ] 1.3 Проверить, что собранный вложенный `shortlinks` запускается (`Shortlinks.app/Contents/MacOS/shortlinks --help`) и работает с тем же хранилищем

## 2. Логика установки в ShortlinksCore

- [ ] 2.1 Добавить `CLIInstaller` с `bundledBinaryURL` (путь к вложенному бинарю от `Bundle.main.bundleURL`) и константой каталога `~/.local/bin`
- [ ] 2.2 Реализовать `status() -> .installed(path)/.notInstalled/.conflict(reason)` через резолв симлинка и сравнение с `bundledBinaryURL`
- [ ] 2.3 Реализовать идемпотентный `install()`: создать `~/.local/bin` при отсутствии, атомарно (пере)создать симлинк, вернуть «конфликт» для постороннего объекта
- [ ] 2.4 Реализовать безопасный `uninstall()`: удалять симлинк только если он указывает на бандл Shortlinks
- [ ] 2.5 Реализовать проверку PATH через `$SHELL -lic 'echo $PATH'` с fail-safe (показывать подсказку при ошибке/таймауте)
- [ ] 2.6 Юнит-тесты на `status/install/uninstall` (первая установка, идемпотентность, перезапись на другой бандл, защита постороннего файла, удаление)

## 3. UI в Настройках приложения

- [ ] 3.1 Добавить раздел «Командная строка» в Настройки: статус, путь, кнопки «Установить CLI» / «Удалить CLI»
- [ ] 3.2 Привязать кнопки к `CLIInstaller.install()/uninstall()` и обновлять отображаемый статус после действия
- [ ] 3.3 Показывать блок-подсказку с готовой строкой `export PATH="$HOME/.local/bin:$PATH"`, когда каталога нет в PATH
- [ ] 3.4 Отрисовать состояния `установлен`/`не установлен`/`конфликт` по существующей теме (`Theme.swift`, акцент `#2A6FDB`)
- [ ] 3.5 Онбординг при первом запуске: показать предложение «Установить» / «Не сейчас», если CLI не установлен; хранить флаг `cliOnboardingShown` в `UserDefaults`, показывать максимум один раз

## 4. Документация и проверка спек

- [ ] 4.1 Обновить `CLAUDE.md`: CLI поставляется внутри app, ставится из Настроек (не отдельное приложение)
- [x] 4.2 Прогнать `openspec validate bundle-cli-with-app` и финальную сборку app
