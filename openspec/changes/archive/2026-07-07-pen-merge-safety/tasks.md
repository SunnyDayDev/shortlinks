# Tasks — Document .pen Merge Safety

## 1. Документация

- [x] 1.1 Создать ветку `docs/pen-merge-safety` от свежего `main`
- [x] 1.2 `CLAUDE.md` (конвенция дизайна): убрать «файл зашифрован — не Read/grep/edit»;
      уточнить, что `.pen` — plain JSON (читается/диффается), Pencil MCP нужен для
      авторских правок ради сохранения инвариантов (`id`/`ref`/схема)
- [x] 1.3 `CLAUDE.md`: добавить краткую модель безопасного слияния `.pen` (условия
      S1–S5 + фолбэк на ручной мерж/выбор варианта; дизайн-система — единый hotspot)

## 2. Спеки, sync и archive (до PR)

- [x] 2.1 `openspec validate pen-merge-safety` — дельты валидны
- [x] 2.2 `/opsx:sync` — влить дельты в главные спеки (`design-file`, `dev-workflow`)
- [x] 2.3 `/opsx:archive` — заархивировать изменение

## 3. (Опционально) валидатор инвариантов

- [x] 3.1 Небольшой скрипт (напр. `design/validate_pen.py`), проверяющий: JSON парсится,
      `id` глобально уникальны, все `$variableName` есть в `variables`, все `ref`
      разрешаются в reusable-`id`. Печатает нарушения — основа пост-merge-проверки
      (проверен на реальном файле → OK; на сломанной копии → ловит все нарушения)

## 4. PR и завершение

- [ ] 4.1 Открыть PR (`docs/pen-merge-safety`) со всем содержимым: `CLAUDE.md`,
      синхронизированные спеки, архив изменения, (опц.) валидатор; дождаться зелёного CI
- [ ] 4.2 После аппрува: merge-commit (`gh pr merge --merge --delete-branch`),
      подтянуть локальный `main`
