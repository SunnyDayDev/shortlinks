## 1. Зафиксировать процесс в документации

- [x] 1.1 Добавить в `CLAUDE.md` раздел «Git-процесс»: работа в ветках, запрет прямых коммитов в `main`, мерж через PR
- [x] 1.2 Описать конвенцию именования веток (`feat|fix|chore|docs/<kebab>`) в `CLAUDE.md`
- [x] 1.3 Описать правило слияния (merge-commit, удаление ветки, ff-обновление локального `main`) и условия мержа (зелёные сборка/`openspec validate`)

## 2. Синхронизировать спеку

- [x] 2.1 Прогнать `openspec validate --specs` для `dev-workflow`
- [ ] 2.2 Синхронизировать дельту `dev-workflow` в `openspec/specs/` (`/opsx:sync`) при завершении

## 3. Опционально: защита ветки

- [ ] 3.1 (Вручную в GitHub) Включить branch protection для `main`: запрет прямого push, требование PR
- [ ] 3.2 Зафиксировать в README/`CLAUDE.md`, включена ли защита, чтобы ожидания совпадали
